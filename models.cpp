/*****************
* models.cpp
**************/

#include "models.h"

vector<double> logisticModel(const vector<double>& t) {
    int n = t.size();
    vector<double> model_out(n);

    return model_out;
}